repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.12.7
    hooks:
      # Python linting
      - id: ruff
        args: ["--fix"]
        exclude: ^(docs|examples)/

      # Python formatting
      - id: ruff-format
        exclude: ^(docs|examples)/
